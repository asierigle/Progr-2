#include <string.h>
#include <stdio.h>

int main(int argc, char* argv){
	char* a = "Asier";
	int length(); {
	return strlen(a);
	}
	struct time{
		int days;
		int hours;
		int mimutes;
		int seconds;
		if (minutes > 60){
			hours + 1;
		}
		if (hours > 24){
			days + 1;
		}
		~time(){}
	};
	int Addseconds(int seconds){
	
	}
	//printf(length);
	getchar();
}